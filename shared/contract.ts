export interface Book {
    title: string;
    author: string;
    description: any;
    cover: any;
    slug: string;
}

export interface Publisher {
    name: string;
    pageContent: any;
    slug: string;
}